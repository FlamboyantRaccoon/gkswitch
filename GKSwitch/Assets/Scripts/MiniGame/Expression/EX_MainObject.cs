using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EX_MainObject : MonoBehaviour
{
    [SerializeField]
    public EX_MoveConfig[] m_hideSpotArray;
    [SerializeField]
    public Sprite[] m_eyeSpritesArray;
    [SerializeField]
    public Sprite[] m_mouthSpritesArray;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
